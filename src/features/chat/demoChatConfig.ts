export const DEMO_CHAT_LIMITS = {
  maxMessageChars: 500,
  maxGraphContextChars: 24_000,
  maxNodeBodyChars: 1_200,
  maxNodes: 50,
  maxEdges: 100,
  maxOutputTokens: 500,
  maxRequestsPerMinute: 12,
} as const;
